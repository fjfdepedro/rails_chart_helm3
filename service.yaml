apiVersion: v1
kind: Service
metadata:
  name: {{ include "micro-services-rails.fullname" . }}
  labels:
    {{- include "micro-services-rails.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "micro-services-rails.selectorLabels" . | nindent 4 }}
